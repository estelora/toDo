app.config(['$routeProvider', function ($routeProvider) {
  //object to define the route
  // var routeDefinition = {
  //   controller: 'ItemCtrl',
  //   controllerAs: 'itemDetail',
  //   templateUrl: 'item-detail.html'
  };

  //angular parses that var here looks something like below
  //$routeProvider.when('/', routeDefinition);
  // $routeProvider.when('/item-detail', routeDefinition);
}]);

